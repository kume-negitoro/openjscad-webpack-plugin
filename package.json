{
  "name": "openjscad-webpack-plugin",
  "version": "1.0.0",
  "description": "Plugin that generates 3D models from bundled JS file for OpenJSCAD",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "kume-negitoro",
  "license": "MIT",
  "dependencies": {
    "@jscad/cli": "^1.10.0"
  },
  "devDependencies": {
    "@types/node": "^14.14.25"
  }
}
